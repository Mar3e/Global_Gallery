enum LottieAnimation {
  dataNotFound("data_not_found"),
  empty("empty"),
  error("error"),
  loading("loading"),
  smallError("small_error");

  final String name;

  const LottieAnimation(
    this.name,
  );
}
